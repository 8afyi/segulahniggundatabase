<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= niggun.title %> | Segulah Niggun Database</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <div class="site-shell">
      <%- include("../partials/site-header", {
        headerAction: { type: "link", href: "/admin/login", label: "Admin" },
        headerMetaText: "Niggun detail card and audio playback."
      }) %>

      <main class="wrap shell-main">
        <p><a href="/" class="button-secondary">Back to list</a></p>

        <article class="panel detail-card">
          <h2><%= niggun.title %></h2>

                    <div>
\            <audio controls preload="metadata" src="/media/<%= niggun.audioPath %>"></audio>
            <% if (niggun.originalFilename) { %>
              <p><small>Source file: <%= niggun.originalFilename %></small></p>
            <% } %>
          </div>


          <p><strong>Singers:</strong> <%= niggun.singers.join(', ') %> / 
          <p><strong>Authors:</strong> <%= niggun.authors.join(', ') %> /
          Uploaded <%= uploadedDateLabel || 'N/A' %></p>
          <div class="tag-row">
            <strong>Tags:</strong>
            <% if (niggun.occasions.length) { %>
              <div class="tag-list">
                <% for (const tag of niggun.occasions) { %>
                  <span class="tag-chip"><%= tag %></span>
                <% } %>
              </div>
            <% } else { %>
              <span>N/A</span>
            <% } %>

            <% if (niggun.prayerTimes.length) { %>
              <div class="tag-list">
                <% for (const tag of niggun.prayerTimes) { %>
                  <span class="tag-chip"><%= tag %></span>
                <% } %>
              </div>
            <% } else { %>
              <span>N/A</span>
            <% } %>
          </div>
          <p><strong>Tempo:</strong> <%= niggun.tempo || 'N/A' %> / 
          <p><strong>Key:</strong> <%= niggun.musicalKey || 'N/A' %></p>

          <% if (niggun.notes) { %>
            <div>
              <p><b>Notes:</b> <%= niggun.notes %></p>
            </div>
          <% } %>

          <% if (niggun.lyrics) { %>
            <div class="lyrics-block">
              <h3>Lyrics</h3>
              <pre class="lyrics-content" dir="rtl" lang="he"><%= niggun.lyrics %></pre>
            </div>
          <% } %>

        </article>
      </main>

      <%- include('../partials/site-footer') %>
    </div>
  </body>
</html>
